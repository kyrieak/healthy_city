<% tag("div", :class => "icon") %>
  <% done ? (opacity_val = 1) : (opacity_val = 0.2) %>
  <%= link_to image_tag(a.filename, :class => "icon", :style => "opacity:#{opacity_val};"),
        user_activity_path(:user_id => a.user.id, :id => a.id ) %>
<% tag("/div") %>