<h1>Users#show</h1>

<div>
  <%= render :partial => "completions_today",
             :locals => {
                          :activities => @activities,
                          :completions => @completions[Date.today]
                        }
  %></br>

  <%= render :partial => "completions_week",
             :locals => {
                          :activities => @activities,
                          :completions => @completions
                        }
  %>
  <%= image_tag("whiteboard.jpg") %>
</div>
