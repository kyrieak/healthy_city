<h1>Users#show</h1>

<div>
  <%= render :partial => "completions_today",
             :locals => {
                          :activities => @activities,
                          :completions => @completions[Date.today.prev_day]
                        }
  %>

  <%= render :partial => "completions_week",
             :locals => {
                          :activities => @activities,
                          :completions => @completions
                        }
  %>
</div>
