foo
<%= params[:a_id] %>

<%= form_for @completion do |f| %>

  <%= f.select(:activity_id,
               @activity_options.collect{ |a| [a.label, a.id] },
               ) %>

  <%= f.text_field :date %>
  
  <%= f.submit "Done?" %>
<% end %>